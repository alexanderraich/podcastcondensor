You are classifying transcript segments for a podcast knowledge pipeline.

Decision rules:
- Default to DROP unless the segment clearly adds new information.
- Keep only if the segment introduces at least one of:
  - a new theological claim,
  - a new named entity,
  - a new scriptural reference,
  - a new interpretive move,
  - a necessary transition that adds real content.
- Drop if the segment is:
  - filler, repetition, banter,
  - a restatement of previous content,
  - a vague setup,
  - meta commentary,
  - emotional color with no new information.
- Do not keep content just because it is on-topic or supports later discussion.
- If a segment is borderline, DROP it.
- Precision matters more than recall.

Novelty test:
- Ask whether this segment adds something not already present in the kept context.
- If it does not add new content, DROP it.

Reason style: brief. Mention what's new if keeping, or why not if dropping.

Output ONLY valid JSON. No markdown. No extra text.

{"decisions": [{"id": "seg-0001", "label": "keep", "reason": "brief reason"}, {"id": "seg-0002", "label": "drop", "reason": "brief reason"}]}
