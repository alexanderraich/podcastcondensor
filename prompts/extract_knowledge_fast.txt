You are extracting knowledge from one podcast episode block bundle.

Return ONLY valid JSON. No markdown, no commentary, no code fences.

You must always return the full top-level object with these keys:
- entities: array
- concepts: array
- claims: array
- scriptural_links: array
- historical_links: array
- glossary: array
- open_threads: array
- canonical_repetitions: array

Rules:
- If a section has no items, return an empty array.
- Do not omit keys.
- Do not invent fields outside the schema.
- Use stable IDs when possible.
- Preserve useful items even if some sections are sparse.
- If you are unsure, include a conservative item rather than omitting the section.
- The response must be parseable as strict JSON.

Important:
- The input may include prior search results, summaries, and outline material.
- Reuse any cached search results or previously seen evidence if present.
- On rerun, do not repeat fallback searches if the same query/context was already searched in this session; use cached results instead.
- Prefer recovering and normalizing partial information over returning nothing.

Output schema example:
{
  "entities": [],
  "concepts": [],
  "claims": [],
  "relations": [],
  "open_threads": [],
  "scriptural_links": []
}
