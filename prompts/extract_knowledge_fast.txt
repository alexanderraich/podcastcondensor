You are extracting structured knowledge from one podcast episode's summaries and outline.

Return ONLY valid JSON. No markdown, no commentary, no code fences.

You must always return the full top-level object with ALL of these keys:
- entities: array of entity objects
- concepts: array of concept objects
- claims: array of claim objects
- scriptural_links: array of scriptural reference objects
- historical_links: array of historical link objects
- glossary: array of glossary term objects
- open_threads: array of open-thread objects
- canonical_repetitions: array of repetition objects

Rules:
- If a section has no items, return an empty array [].
- Do not omit any key.
- Do not invent fields outside the schema.
- All values must come from the input text; do not invent entities or concepts.
- Use stable lowercase_snake_case IDs (e.g. "divine_simplicity", "temple_worship").
- Prefer recovering and normalizing partial information over returning nothing.
- Extract claims when the text makes a substantive theological, historical, or interpretive assertion. Do not treat descriptions of what someone said as claims unless they are presented as the podcast's own position.
- The response must be parseable as strict JSON.

Required field formats for each object type:

entities — each entity object must have:
  "id": "lowercase_snake_case_unique_id"
  "title": "Full Name or Title of the entity (max 80 chars)"
  "category": one of "person", "place", "group", "angelic_being", "divine_person", "writings", "liturgical_feast", "object", "theological_concept"
  "summary": "1-2 sentence description of who or what this is (max 200 chars)"
  "episode_numbers": [single episode number as integer]
  If a person or entity appears multiple times, use one entry and list all episode numbers.

concepts — each concept object must have:
  "id": "lowercase_snake_case_id"
  "title": "Short name (max 80 chars)"
  "summary": "1-2 sentence explanation (max 200 chars)"
  "episode_numbers": [integer]

claims — each claim object must have:
  "id": "lowercase_snake_case_id"
  "text": "The claim statement — a full sentence expressing a theological, historical, or interpretive position (max 300 chars)"
  "topic": "Short topic label, e.g. 'Christology', 'Soteriology', 'Angelology', 'Ecclesiology', 'Scripture', 'Liturgy', 'History', 'Moral Theology', 'Eschatology', or 'Other'"
  "supporting_reference": "Optional — scripture reference, council, father, or source that supports the claim. Empty string if none."
  "episode_numbers": [integer]

scriptural_links — each link must have:
  "reference": "Book Chapter:Verse (e.g. Genesis 1:1)"
  "summary": "How it is used (max 200 chars)"
  "episode_numbers": [integer]

historical_links — each link must have:
  "title": "Event, council, period name (max 80 chars)"
  "summary": "How it relates (max 200 chars)"
  "episode_numbers": [integer]

glossary — each term must have:
  "term": "Word or phrase (max 80 chars)"
  "definition": "Definition (max 200 chars)"
  "language": "Language of origin (e.g. Greek, Hebrew, English)"
  "episode_numbers": [integer]

open_threads — each thread must have:
  "title": "Topic label (max 80 chars)"
  "summary": "What was left open (max 200 chars)"
  "episode_numbers": [integer]

canonical_repetitions — each must have:
  "title": "Topic label (max 80 chars)"
  "summary": "What is frequently repeated (max 200 chars)"
  "episode_numbers": [integer]

Output schema example (use this exact structure; fill arrays with properly typed objects):
{
  "entities": [
    {"id": "adam", "title": "Adam", "category": "person", "summary": "First man created by God, whose sin introduced death into the world", "episode_numbers": [21]}
  ],
  "concepts": [
    {"id": "original_sin", "title": "Original Sin", "summary": "The doctrine that Adam's sin corrupted human nature and is passed to all descendants", "episode_numbers": [21]}
  ],
  "claims": [
    {"id": "temple_as_microcosm", "text": "The Jerusalem temple was understood as a microcosm of the created order, with the holy of holies representing heaven and the outer courts representing the earth.", "topic": "Scripture", "supporting_reference": "Exodus 25-40, Temple structure", "episode_numbers": [21]}
  ],
  "scriptural_links": [],
  "historical_links": [],
  "glossary": [],
  "open_threads": [],
  "canonical_repetitions": []
}
