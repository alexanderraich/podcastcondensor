# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
*.whl
venv/

# Output artifacts — SRTs, per-episode global_state, and universe/super-cut/
# ultra-cut caches ARE version-controlled (LLM-generated, expensive to
# regenerate). Everything else (audio, diag logs, temp) stays ignored.
output/*
!output/*.gitkeep
!output/universe_state_*.json
!output/super_cut_*.json
!output/ultra_insights_*.json
!output/ultra_outline_*.json
!output/ultra_cut_narration.txt

# Per-episode: whisper SRT + global_state.json (LLM extraction) + narration.txt
# (LLM narration) tracked, plus the deep-narration artefacts (ep-NNN_arc_deep
# .json, ep-NNN_narration_deep.txt) which are LLM-generated and expensive to
# regenerate. Audio (mp3), TTS renders and diag logs stay ignored.
# NOTE: must un-ignore the ep dirs BEFORE re-ignoring their contents, else
# git never descends into them and the negations below never fire.
!output/ep-*/
output/ep-*/*
!output/ep-*/source_subtitles.srt
!output/ep-*/global_state.json
!output/ep-*/narration.txt
!output/ep-*/ep-*_arc_deep.json
!output/ep-*/ep-*_narration_deep.txt

# Temp
*.tmp
*.temp

# Pytest cache
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Local ffmpeg/yt-dlp binaries
.local/

# Claude Code local settings
.claude/settings.local.json

# Debug directory
src/output/
